<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>CobraBridge.Bridge</RootNamespace>
  </PropertyGroup>

  <!--
    Phase 2 scaffold: the anti-corruption layer.
    Translates fixed-width COBOL account records into modern domain
    models and exposes them over a clean HTTP/JSON API. No external
    NuGet packages yet — kept buildable from the base SDK.
  -->

</Project>
